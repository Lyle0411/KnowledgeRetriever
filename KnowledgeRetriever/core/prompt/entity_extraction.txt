我想要构建知识图谱，请从以下法规条款中提取法律要素关键词，作为相关核心实体，遵循以下指南：
- 包含违规情形、技术要求等核心要素
- 区分法律主体和禁止性规定
- 只提炼出最核心、出现频率最高的违纪事实相关的实体，不包括具体人名，地名等
- 对多义词进行消歧（如"测试状态"需区分合规测试与违规规避）
- 识别隐性关联（如"分时段屏蔽"隐含"蓄意违规"特征）
- 标注程度副词（"频繁"、"多次"等修饰语需保留）
- 不必提取客观名词，只关注和违纪事实相关的行为或名词
- 重点关注和违纪行为相关的实体，要求提炼出的关键词能够尽可能的概括主体内涵
- 请严格判断，如果找到的实体不能反映所有线索整体反映的主题，或者明显无意义的重复线索，无需提取实体，entities请返回空列表
- 将简写的实体名补充为完整形式
- 提取的实体需要和话题或线索整体反映的主题紧密相关。请提取具体的、有实际意义的实体，避免空泛的概念，实体应该是独立的名词形式

输出格式：
   以 JSON 格式输出结果，结构如下：
   ```json
   {
     "entities": ["实体1", "实体2"...]
   }
   ```

请分析给定的文本，并直接输出json，无需额外的解释说明。